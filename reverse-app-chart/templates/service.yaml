apiVersion: v1
kind: Service
metadata:
  name: reverse-app-chart
  labels:
    {{- include "reverse-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      # name: http
      nodePort: {{ .Values.service.nodePort }}
  selector:
    {{- include "reverse-app-chart.selectorLabels" . | nindent 4 }}
